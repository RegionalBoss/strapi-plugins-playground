{
  "extends": "../../../tsconfig.base.json",

  "compilerOptions": {
    "outDir": "../../../build"
  },

  "include": [
    // Include the root directory
    "server",
    // Force the JSON files in the src folder to be included
    "server/**/*.json",
    "admin/**/*",
    "admin/src/translations/*.json",
    "strapi-server.*",
    "strapi-admin.*",
    "setup-package.*",
    "permissions.*"
  ]
}
